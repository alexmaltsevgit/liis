export type AppTheme = {
  colors: {
    dark: string;
    light: string;

    background: string;

    red: string;
    green: string;
    yellow: string;

    text: {
      primary: string;
      secondary: string;
    };
  };

  breakpoints: {
    lg: string;
    md: string;
    sm: string;
  };
};
